part of'swap_widgets_imports.dart';

class BuildSwapInfo extends StatelessWidget {
  const BuildSwapInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
